"""
B5 — TradingAgents integration placeholder.

When built:
  - Copy relevant agents from legacy/fork/agents/ into src/desks/equity_ls/core/
  - Run them here (vendor, don't import from legacy/)
  - Write outputs to B4: kb.add_agent_output(ticker, agent_name, signal, reasoning)
  - data_sources.py Section 8 (get_trading_agent_opinion) reads from B4

Storage: trading_agent_outputs table in B4 knowledge_base.db (already built).
Execution code: src/desks/equity_ls/core/trading_agents.py (to be built with A-functions).
"""
