# 工作日志 · AI Investment System — Build Log (Status Tracker)

> Architecture: **`src/desks/MASTER.md`**. This log says *where each piece is + what's next*.
> **Rule:** after every change, update the matching line here (status + location), and add a line to the Decision Log.
> Status key: ✅ done · 🟡 partial · 🔴 not built · ⏸ parked · 🔁 needs migration onto new model
> Last updated: 2026-07-14

> **⚠️ 2026-06-30 — two pivots happened. Read before trusting old paths below:**
> 1. **大清理:** dead code moved out of `src/`. Any `src/core|features|adapters|services/...`
>    path in entries below now lives under **`legacy/superseded/`**; any `src/agents/...`
>    under **`legacy/fork/`**. The live bot (`src/tools/`) is unchanged. See `legacy/README.md`.
> 2. **Model pivot:** the 6-desk "Spine" model (entries before 2026-06-30) was superseded by the
>    **asset-class desk model** (`src/desks/`). Old-model docs are in `docs/archive/`. Those entries
>    are kept as history; the active backlog is the asset-class desks.

---

## CURRENT SPRINT — asset-class desk model (strangler; bot stays live)
Build order: `equity_ls` → `quant` (signal service) → `macro` → `pm_risk` → remaining desks.
Each desk: infrastructure (B) → core functions (A) → risk layer (C). See `src/desks/MASTER.md`.

- ✅ Desk foundation: `IdeaCard` contract + `Desk` base + registry (`src/desks/base.py`, `contracts.py`, `registry.py`)
- ✅ **equity_ls B1** — universe filter + tier tag (`TickerMeta`) + SQLite exclusion DB (`infrastructure/b1_universe/`)
- ✅ **equity_ls B2** — 8-section data layer, yfinance + OpenBB cross-check, all 8 sections live (`infrastructure/b2_data_source/`)
- ✅ **equity_ls B3** — portfolio DB: holdings/watchlist/journal + sector exposure; Notion seed bridge (`infrastructure/b3_portfolio/`)
- ✅ **equity_ls B4** — knowledge base: reports + FTS search, thesis versions, agent outputs, PDF inbox ingestion (`infrastructure/b4_knowledge_base/`)
- ✅ **equity_ls B5** — vendored TradingAgents pipeline: 4 analysts ∥ + bull/bear debate + research manager + trader → saves to B4 (`infrastructure/b5_trading_agents/`)
- ✅ **equity_ls screener** — 6-step methodology (hard filters → peer group → 7 engines → composite /100 → classification → handoff) + Step-7 cadence config (`core/screener/`)
- ✅ **equity_ls A2** — single-name deep dive: gate → screener → B5 run → desk conclusion → valuation w/ live peer table → trade expression → canonical verdict (`core/a2_deep_dive/`)
- 🟡 **equity_ls monitor** — daily loop w/ per-ticker last-run state, cadence-driven refresh, deep-dive escalation, Telegram alerts (`core/monitor/`) — built, not yet on a cron
- ✅ **2026-07-02 audit** — full code audit of the desk: 9 real bugs fixed (monitor never screened holdings; 12M momentum never computed; earnings-date key mismatch killed catalyst+event triggers; SEC field mapping wrong; B5 read SEC shape wrong; FTS delete corrupted index; nondeterministic "latest" rows; DB connection leaks; multiline verdicts broke alert logic) + shared LLM factory w/ timeout/retries + 40 unit tests (`tests/test_equity_ls_desk.py`)
- ✅ **2026-07 spec + audit round 2** — full capability-based spec written for the desk (Theme/Name Discovery, Deep Dive, Catalyst Response, Portfolio Decision Support, Relative Value; B1 Discovery Pool, B4 Decision History, B5 run modes; C-layer 8 risk checks). Audit before build found 2 more real bugs, both fixed: `debt_to_equity` was in percentage-point form from yfinance (KO=124.9 ≠ 124.9x, needed /100) so low-debt names like NVDA were flagged high-leverage — fixed at the source in `b2_data_source/data_sources.py`; screener `_handoff()` and `cadence.should_trigger_deep_dive()` were two independent authorities that silently disagreed on when T0/T1 names escalate to a deep dive — `_handoff()` now delegates to cadence as the single authority, monitor reads the result instead of recomputing it. 43 tests passing.
- 🟡 **Backstage-first build order** (per Louis) — pure logic + correctness first (no cron/Telegram wiring yet); frontstage wiring is a deliberate later pass.
  - ✅ **Decision History + single-writer rule** — new `infrastructure/b4_knowledge_base/decision_history.py`. `decisions` = append-only log any A-function may write; `current_view` = one official stance per ticker, settable only by whichever of A2 (not-held)/A4 (held) owns it right now (ownership resolved dynamically via `portfolio_db.is_holding`, not stored). A3/A5 can never set it directly — enforced via `SingleWriterViolation`, not just documented. Wired into A2's `run()`. 9 tests.
  - ✅ **Currency tags + minimal C risk layer** — `universe.currency_for()` derives currency from ticker suffix (found + fixed a real bug in passing: TPZ.TO was rejected by the universe gate, `.TO`/`.V` were never in `ELIGIBLE_SUFFIXES` despite the code comment claiming otherwise). New `core/risk/checks.py`: concentration (per-currency-book, not mixed — flags stale-price fallback instead of hiding it), liquidity (days-to-exit at 100% ADV), data quality (critical-field + scoring-field coverage). Confirmed live against the real book: yfinance currently can't price Zhipu or the 2x SK Hynix ETF — exactly the two largest real concentration risks (30%/23% of the HK book) — so the stale-price flag is catching a real, present gap, not a hypothetical one. Also confirmed: `portfolio_db.account` has never been populated (Notion has no Account field) — sidestepped via `currency_for()`, Notion sync itself not fixed (separate scope). 26 tests. Noted for later: yfinance returned `None` prices under this session's rapid concurrent calls — looks like rate-limiting, a real risk for the future monitor cron fetching dozens of names at once; not fixed now.
  - ✅ **equity_ls A3** — Catalyst Response (`core/a3_catalyst_response/`). One LLM judgment call (vs A2's ~15-20) across 3 event types (earnings_preview/earnings_review/catalyst); dedupe window (default 1d, skips reprocessing the same ticker+event_type) and a portfolio-wide daily rerun budget (default 5/day) built in from day one per audit finding #8 — earnings season can put 15-20 holdings' events in the same window, and an uncapped A3 could fire the whole book through TradingAgents at once. Material events (thesis Weakens/Breaks) escalate to a full A2 rerun, budget permitting; exhausted budget surfaces as an error for manual review rather than silently dropping. A3 never sets `current_view` — always `record_decision(source="a3")`, respecting the single-writer rule. Read-through reuses the shared peer map. 18 tests.
  - ✅ **Peer map extracted to A5** — `_PEER_MAP`/`_SECTOR_FALLBACK`/peer-table logic moved out of `a2_deep_dive/deep_dive.py` into `core/a5_relative_value/peers.py`, so A2's valuation view, A3's read-through, and eventual A5 itself share one map instead of drifting into separate copies.
  - ✅ **equity_ls A4** — Portfolio Decision Support (`core/a4_portfolio_decision/`). Sole writer of `current_view` for held tickers (the A4 half of the single-writer rule). **Hard boundary (audit finding #4, pm_risk doesn't exist yet):** direction only — Hold/Add/Trim/Sell/Rotate/Deep Dive Further — never a share count, dollar amount, or target weight; enforced structurally (`PositionReview` has no size/weight field — guarded by `test_position_review_has_no_sizing_field`) not just by prompt instruction. `review_position(ticker)` refuses to run on anything not actually held (that's A2's job); pulls holding P&L, saved thesis, recent A2/A3 signals, fresh screener score, C-layer risk flags → one LLM judgment → becomes `current_view`. `review_portfolio()` is read-only: concentration scan + sector/currency exposure by live market value + an LLM strategy note; never touches any ticker's `current_view`. 8 tests.
  - **118 tests passing repo-wide** (all network/LLM calls mocked in tests; live bot's 46 tools unaffected throughout).
  - 🔴 Remaining: A1 (Theme Discovery), A5 proper (pair/basket construction — `peers.py` is the data layer, the decision logic isn't built), full 8-check C layer (Thesis/Valuation/Event/Correlation/Portfolio Relevance still missing), B5 run modes (only "full deep dive" mode exists — Thesis Review/Earnings/Position Review/Second Opinion modes from the spec aren't built), B1 Discovery Pool, frontstage wiring (cron, Telegram commands, alert formatting — deliberately deferred until backstage is proven).
  - 🔴 Next: A3 (Catalyst Response) → A4 (Portfolio Decision Support) → A1/A5/B5 run modes.
- 🔴 equity_ls A1 (theme discovery) · A3 (catalyst response) · A4 (portfolio decision support) · A5 (relative value) · C (risk layer) · Decision History · monitor cron wiring

---

## ARCHIVE — prior 6-desk "Spine" model (superseded 2026-06-30, kept as history)

---

## CURRENT SPRINT — migration to the new model (strangler; bot stays live)

1. ✅ Write canonical objects (§3.1) as code — the shared language. → `src/core/objects.py`
2. 🟡 Rebuild the **morning briefing** on spine + objects as the reference implementation. → `src/features/morning_briefing.py` (standalone; not wired to live scheduler)
3. 🟡 Build **Coverage Analyst** skeleton (richest desk; closes the thesis-health / sell-discipline gap). → types + lenses + synthesize + orchestrator built & verified (`src/features/coverage/`). REMAINING: vendor lens A (TradingAgents fundamentals) + lens B (ai-hedge-fund Damodaran) — licence check needed; real evidence assembly; wire triggers (touches live scheduler).
4. 🔁 Migrate remaining tools onto the desks one at a time; commit after each.
5. 🔴 Retire the four old docs once everything is migrated; this pair becomes the only source of truth.

> Next action right now: **Coverage Analyst spec** (six fields + A/B/C sources), then objects.

---

## PART 1 · THE SPINE — function status

### Intelligence
- ✅ Macro & geo scan — geopolitical pulse, morning briefing
- ✅ News filter — Tavily + DeepSeek score, breaking ≥8/10 (2hr)
- ✅ Sector / theme monitor — theme radar (55 ETF), sector rotation
- 🟡 FICC / commodities / crypto feeds — built but shallow (FRED data, futures, CoinGecko prices)

### Research
- ✅ Deep dive (9-section)
- ✅ Valuation — P/E, EV/EBITDA, peers · 🔴 DCF socket
- 🟡 Thesis check — exists in deep dive; **🔴 proactive thesis-health watch not built** (the exit gap)
- ✅ Earnings transcript + reaction
- ✅ SEC filings
- 🟡 Report ingestion — research library (SQLite) built; **🔴 PDF ingestion not built**

### Decision
- ✅ Conviction + sizing (size_position)
- ✅ 腾位置 / portfolio advisor
- ✅ Entry points (tiered)
- 🟡 House-view formation — synthesize_view built on desk model; orchestrator not yet wired
- ✅ Quant screen / optimise / backtest (V3 Quant, 6 tools)

### Monitoring
- ✅ Portfolio + watchlist P&L
- ✅ Price / target alerts
- ✅ Catalyst calendar
- 🟡 Thesis-health watch — verdict state machine built, lenses A/B real, triggers not wired

### Review
- ✅ Monthly 复盘
- ✅ Decision journal (auto on trades)
- 🔴 Recommendation accuracy tracking

### Risk (cross-cutting)
- ✅ Concentration / correlation / drawdown (Phase 1); 🟡 decision cap built on desk model
- 🟡 Macro regime detector — built; **🔴 not wired into decisions**
- 🔴 VaR / macro stress test (Phase 2)

---

## PART 2 · THE DESKS — spec + build status

| Desk | Spec written | Built (legacy) | Migrated to model | Sources vendored | Notes |
|---|---|---|---|---|---|
| Research Librarian | ✅ | 🟡 library only | 🔴 | 🔴 LlamaIndex | PDF ingestion missing |
| **Coverage Analyst** | ✅ | ✅ deep dive/val | ✅ | ✅ TradingAgents + ai-hedge-fund | on-demand model path verified; triggers pending |
| Idea Scout | ✅ | ✅ radar/proactive | 🔴 | 🔴 OpenBB | multi-feed, not multi-opinion |
| House View (CIO) | ✅ | ✅ personas/shadow | 🟡 | 🟡 ai-hedge-fund + TradingAgents | on-demand A/B/C + PM View built; triggers/journal pending |
| Quant Engine | ✅ | ✅ V3 Quant | 🟡 | 🔴 Qlib | on-demand rank signals built; optimiser/backtest/routing pending |
| Risk Watch | ✅ | ✅ Phase 1 | 🟡 | 🔴 (reference only) | on-demand sweep + decision cap built; routing pending |

> "Spec written" = full six-field block agreed. Blueprint currently holds first-pass stubs; per-desk deep specs are the next work item.

---

## PART 3 · STANDARDS & SUPPORT — status

- ✅ Canonical objects (§3.1) — coded as dataclasses + enums in `src/core/objects.py` (7 objects, 6 enums, `to_dict` serialiser). Zero data-SDK imports (stable core).
- 🔴 Multi-source policy — agreed; modules not yet built.
- 🟡 Sources registry — listed; licenses to verify; nothing vendored yet.
- ✅ ONBOARDING.md added to root — single entry point; read order points to AGENTS → BLUEPRINT → BUILD_LOG → desk spec.
- ✅ WORK_ORDER.md written — executable, literal task list for a less-capable agent (infra → adapters → Coverage Analyst), each task self-contained with verification + Build Log wording.
- ✅ Communication & Delivery — Telegram/Notion live; timings live.
- 🟡 Systematic standards — templates exist ad-hoc; **🔴 not centralised.**
- 🟡 Knowledge — static playbooks partial; dynamic learning 🔴.
- 🟡 Operating discipline — adapter base now enforces declared `on_failure`; dependency direction checks not yet automated.
- 🟡 Infrastructure — adapters/storage/runtime live; **🔴 registry + dispatcher not built** (desks are hard-wired, not declared).
- 🟡 Coverage tag — equities deep; FICC/crypto shallow.

---

## DECISION LOG (newest first)

- **2026-07-28** — Louis asked why MU and WDC got contradictory "thesis intact"/"thesis concern" verdicts in the same 8%+ move alert despite being closely related memory-cycle names. Audit found: both have an empty `thesis` field in Notion (confirmed 20 of 47 held positions, 43%, lack one) — `_thesis_verdict()`'s fallback path runs a fully independent Tavily search + isolated LLM call per ticker with zero cross-ticker awareness, so two correlated names can get opposite-sounding reads purely from which specific articles each search happened to surface. Two fixes: (1) code — `_group_context_for_batch()`: when 2+ thesis-less tickers in the same alert batch share a `PORTFOLIO_CATEGORIES` bucket, one shared sector search grounds all of them instead of N independent ones; verified against the exact reported batch (MU+WDC→AI-Chips, SEI+BE→AI-Energy grouped correctly, LITE alone in AI-Infrastructure this batch still falls back solo, as intended). (2) data — backfilled real, differentiated `Thesis (Durable)` text in Notion for MU (HBM/DRAM leadership), WDC (enterprise HDD, AI cold-storage tier, distinct from NAND), and LITE (AI optical/CPO supply chain) — the 3 highest-frequency alert names among the 20 missing one; verified readable through the same `notion_holdings.reload_holdings()` path the bot uses. 17 more held positions still lack a thesis, lower priority. 118 tests passing · `src/tools/scheduler.py`, Notion
- **2026-07-28** — Live bug caught by Louis: "this US market alert in the middle of the HK day which is US market close time? doesn't make sense" — the HK Close message showed "08:05" (should be 16:05 HKT) alongside a US-ticker (IONQ/SNDK) price alert that had no business firing during HK hours. Two real bugs: (1) `check_price_alerts()` checked every held ticker as long as ANY market was open anywhere, not gated to each ticker's own home market — a US ticker's last-completed-session move could surface hours later during HK trading, looking like a live event when it wasn't. Added `_ticker_market()` (suffix-based, crypto always-on) and gated `tickers_to_check` accordingly — verified during simulated HK-only hours, IONQ/SNDK now correctly excluded while 0700.HK/2513.HK/BTC are included. (2) 3 message headers ("{market} Close", "Price Stabilisation Alert", "Alert Check") used raw `datetime.now()` (server/UTC) instead of the `_now_hkt_str()` helper built specifically for this class of bug back on 2026-07-11 — never applied to these 3 call sites. All switched to `_now_hkt_str()`. 118 tests passing · `src/tools/scheduler.py`
- **2026-07-16** — Louis asked for tappable deep-dive buttons wherever an alert features a single ticker or a few. Found 4 alert types telling the user to manually type a follow-up instead: 8%+ price alert, custom threshold alert, watchlist target hit, price-stabilisation alert — the last one referenced "portfolio advisor [ticker]", which isn't even a recognized command (would've fallen through to the LLM agent as free text). All 4 now send tappable buttons via the existing `deepdive:` callback (same one the main menu's Deep Dive button already uses) · `src/tools/scheduler.py`
- **2026-07-16** — Live bug caught by Louis: "Deep dive Zhipu" (2513.HK, a real held position) returned "Zhipu is private, no public ticker" — confidently wrong. Two compounding causes found: (1) `_fetch_batch_fast()` in prices.py — `yf.download(..., group_by="ticker")` returns a `(Ticker, Price)` MultiIndex even for a single ticker, but the single-ticker branch did `data["Close"]` directly (KeyError, "Close" is never top-level), silently caught and returned `{}`. This meant `get_live_prices()` — the default/fast path used by price alerts, shadow-portfolio P&L, and deep_dive's live-data context — had been returning empty data for EVERY ticker, always, not just Zhipu; confirmed other `yf.download` call sites (risk.py, theme_radar.py, quant/backtest.py, quant/optimizer.py) use the default grouping and were unaffected. (2) `deep_dive`'s ticker arg was passed straight from the LLM's own guess with no resolution against the user's actual holdings — DeepSeek's stale training knowledge assumed Zhipu was still private, and with zero live data available (bug 1) had nothing to correct it. Added `_resolve_ticker()`, checking the loaded watchlist (which already has the correct 2513.HK mapping) before trusting the LLM's guess. Verified end-to-end: "Zhipu" → 2513.HK → live price $1042.0/-18.66%, matching the exact alert that surfaced this. 118 tests passing · `src/tools/prices.py`, `src/tools/telegram_bot.py`
- **2026-07-16** — Live bug caught by Louis: "Add it to watchlist and do a deep dive on it" → bot replied "✅ IT added to watchlist" — the terse add-command regex captured pronoun "it" as ticker IT (Gartner), swallowed the rest of the sentence as the company name (Notion row created: Ticker=IT, Name="And Do A Deep Dive On It"), and silently dropped the deep-dive intent. Fixed with pronoun/compound guards on all quick-command matchers (add/sell/buy/rate/thesis/alert/target): pronoun tickers or "...and/then/also..." trails neutralize the match so the message falls through to the LLM agent (conversation memory resolves "it"). Also added missing agent tool `add_ticker_to_watchlist` — agent could read but not write the watchlist, so fallthrough alone would've been talk with no action. Tool count 46→47 (AGENTS.md smoke check updated). Bogus Notion IT row repurposed into the correct ON Semiconductor entry with the bot's own verdict (PASS at $87, revisit <$70). 118 tests passing · `src/tools/telegram_bot.py`, `AGENTS.md`, Notion
- **2026-07-15** — Same bug recurred after the persistence fix below — deploy/timing confirmed it wasn't a restart-wipe or cross-market overwrite (no restart since prior day, tap was 38min after close, well inside TTL), so a third distinct cause. Real root cause: Section 1 (summary, drives which buttons appear) and Section 2 (freeform per-ticker detail blocks) are two independently LLM-formatted outputs from the same call — if Section 2's `TICKER:\n` header format drifts even slightly on a given run, that ticker silently gets no parsed detail while its button (built separately from Section 1's bold tags) still appears. A structurally guaranteed dead-end regardless of storage layer, since there was never data to store for that ticker. Fixed by falling back to the ticker's own summary bullet line whenever Section 2 parsing misses it — every button that appears is now guaranteed real backing detail. Verified against a reproduction of the exact failure mode. Also attempted an SSH read into the live container to inspect the actual DB state during diagnosis — blocked by the permission classifier as an unauthorized production-read action; diagnosed and fixed from logs/timing/code reasoning instead. 118 tests passing · `src/tools/scheduler.py`
- **2026-07-15** — Live bug caught by Louis: tapping a ticker button on the AI Shadow Portfolio (HK close, 16:05) returned "No detail available — it may have expired" ~1h46m later. Root cause: `_shadow_ticker_detail` (scheduler.py) was a bare in-process dict, never persisted, wholesale-replaced on every `_shadow_portfolio_message()` call. Two real bugs found: (1) any process restart (crash, OOM, redeploy — including the OOM fix deployed earlier the same day) silently wiped all buttons for the rest of the day even though the Telegram buttons still looked live; (2) a later market's close overwrote an earlier market's still-fresh details (dict reset to `{}`, not merged) even with no crash. Moved storage to SQLite (writes to the same Railway volume the checkpointer now uses), upserted per-ticker, 24h TTL on reads. 118 tests passing · `src/tools/scheduler.py`, `src/tools/telegram_bot.py`
- **2026-07-14** — Live bug: Railway flagged 2 OOM restarts on the worker (~19:32-20:05 UTC). Root cause: `langgraph-checkpoint-sqlite` was referenced in code but never actually declared as a dependency, so every deploy silently used LangGraph's `MemorySaver` — unbounded, every conversation thread's full history kept in RAM for the process lifetime. Added the real dependency (pinned `>=2.0,<3.0` for compat with langgraph 0.2.56's `langgraph-checkpoint>=2.0.4,<3.0.0`); also fixed a second latent bug it exposed — `SqliteSaver.from_conn_string()` is a `@contextmanager`, was being used without `with`, binding `memory` to a generator object instead of a usable saver. Fixed by connecting directly via `sqlite3.connect()` and constructing `SqliteSaver(conn)`. Also created and attached a Railway volume (`worker-volume`, 500MB) at `/app/data` — deploy confirmed live: `💾 Memory: SQLite (/app/data/agent_memory.db) — ✅ PERSISTENT (Railway Volume)`. Also discovered the Railway project is internally named `agile-mindfulness` (never renamed from its auto-generated name) — rename needs the dashboard, CLI has no rename command · `src/tools/telegram_bot.py`, `pyproject.toml`
- **2026-07-14** — Meituan-W (3690.HK, 400 sh) zeroed out in Notion Holdings — confirmed closed by Louis, DB was just never updated. Notion.
- **2026-07-14** — Fixed `PORTFOLIO_CATEGORIES` miscategorization flagged 2026-07-06. CBRS was unidentified in Macro/Index/Hedges — resolved via yfinance lookup: it's Cerebras Systems Inc (real AI-chip equity), moved to AI-Chips. Xiaomi (1810.HK) moved China Internet → EV & Future Mobility; IREN moved Crypto & Digital Assets → AI-Energy, both confirmed with Louis · `src/tools/scheduler.py`
- **2026-07-14** — HK real-account holdings sync: Louis's real HK book (Zijin Mining, SK Hynix 2x ETN, Kuaishou, Tencent, Zhipu resized; JD Health, AIA, Alibaba-W, Ping An closed to 0) was stale in the Notion Holdings DB — updated directly to match. Also fixed the yfinance-under-concurrency gap flagged in the 2026-07-08 entry (never fixed at the time): added retry-with-backoff (empty result treated as retryable, not just exceptions) to `_yf_fetch()` and `_fetch_one_detailed()`, since A4's `review_portfolio()` and A5's peer-table both fan out across ~10 concurrent tickers and the future monitor cron will fan out across the full universe. 118 tests passing · `src/desks/equity_ls/infrastructure/b2_data_source/data_sources.py`, `src/tools/prices.py`
- **2026-07-14** — Louis adopted a new L1-L7 + Gate C pipeline architecture (from an external brainstorm doc set) to replace the equity_ls A1-A5 layer naming, no argument invited on the decision itself. Key structural change from current build: A2 (deep-dive monolith, forms `current_view` automatically) is retired; the new L4 decision step is manual (Louis synthesizes L2 research doc + L3 Vibe-Trading + Gate C TradingAgents by hand) rather than code-owned. Two human gates confirmed: after L2 (Louis picks which names advance) and after L3/Gate C (Louis makes the final call) — Vibe-Trading positioned as load-bearing, not sandbox-only, contrary to this session's earlier security-caution recommendation. Standards/thresholds per layer (screener score bands, evidence minimums, backtest pass bar, risk-check thresholds) being defined before any build starts — not yet built.

- **2026-07-11** — Live bug, caught by Louis in real time: HK close alert fired on Saturday 11 Jul 2026, full portfolio summary + AI Shadow Portfolio synthesis on a day the market wasn't open. Root cause: `_is_market_holiday()` only checks the `holidays` package (public holidays), has zero concept of ordinary weekends; the close-alert cron (`schedule.every().day.at(...)`) fires every calendar day with no weekday filter. `send_market_open_alert()` had the identical gap in the function itself but was accidentally safe because its cron registration lists Mon-Fri explicitly — pure luck, not a real guard. Added `_is_weekend()`, wired into both functions ahead of the holiday check (defense-in-depth at the function level, not just cron syntax). Verified against the exact failing timestamp · `src/tools/scheduler.py`

- **2026-07-09** — Louis asked to stop needing manual "reload" in Telegram. Added `auto_reload_holdings()` to the 30-min scheduler loop (same cadence as price alerts): pulls Notion once, mutates `scheduler.WATCHLIST_DATA` and `telegram_bot.WATCHLIST/PORTFOLIO/WATCHLIST_ONLY/WATCHLIST_TICKERS` in place across both modules (they'd been two independently-loaded copies with no sync path between them — the root cause of the earlier HK Close staleness question). Verified live: both modules see new tickers within one call · `src/tools/scheduler.py`

- **2026-07-09** — Louis flagged the live "AI Shadow Portfolio — HK Close" Telegram card showing US tickers (LRCX/GOOGL/MU) and buttons instead of HK-only names. Found 3 compounding bugs in `_shadow_portfolio_message`: (1) it received the full unfiltered cross-market `held` dict instead of the market-filtered ticker list already computed for the main close message — no market boundary existed in the LLM prompt at all; (2) the prompt never asked for company names even though they were already in context, so output was bare tickers; (3) both output-parsing regexes were `[A-Z]{1,6}` — pure-letter only — so HK tickers (digit-led, `.HK` suffix) and index tickers (`^HSI`) silently never got a detail button even when they appeared bold in the text. All three fixed and verified live (HK filter now returns exactly the 10 HK-held names, zero leakage). Also clarified for Louis: the live bot's Notion cache only refreshes on explicit reload, not automatically — today's Notion edits (HK ticker fixes, trades) were made via direct API calls outside the bot's own write-back tools, so the running Railway process won't see them until "reload" is sent in Telegram · `src/tools/scheduler.py`

- **2026-07-09** — First live use of the equity_ls backstage build against the real portfolio (A4 review_portfolio + review_position, screener). Surfaced two real findings: (1) the entire HK book (19 tickers, held + watchlist) has been unanalyzable since being added — 9 had a spurious extra leading zero (yfinance needs exact 4-digit HKEX codes), 3 resolved to entirely wrong companies when naively stripped (COSCO/AviChina/Tongcheng were pointing at unrelated tickers), 1 wasn't HK-listed at all (Roborock is Shanghai STAR Market), 1 index ticker was in the wrong format (Hang Seng needs `^HSI`, not `800000.HK`) — all fixed in Notion + re-seeded, verified live with real market_cap/P-E now flowing. (2) A4's "Sell" verdicts on 4 real HK holdings during this test were confirmed to be data-quality artifacts (missing market_cap → hard filter fail → score 0), not real judgments — correctly did NOT pass these to Louis as advice. Also archived a pre-existing duplicate Zhipu row in Notion labeled "⚠️ DUPLICATE - DELETE ME" from an earlier session that was never cleaned up · `src/tools/scheduler.py`, Notion

- **2026-07-08** — equity_ls backstage build completed (Decision History → currency+C-layer → A3 → A4, per the sequencing agreed same day). Single-writer rule now live and enforced in code: `decision_history.py`'s `current_view` table can only be set by whichever of A2 (not-held)/A4 (held) owns a ticker right now, raising `SingleWriterViolation` otherwise — resolves the 3-way A2/A3/A4 collision the audit flagged before any of A3/A4 existed to actually collide. A3 (Catalyst Response) built cheap-by-design (1 LLM call vs A2's ~15-20) with a daily rerun budget + dedupe window so earnings season can't runaway-fire the whole book through TradingAgents. A4 (Portfolio Decision Support) built with the sizing boundary enforced structurally, not just documented — `PositionReview` has no field a size could go into, guarded by a permanent test. Minimal C risk layer (concentration/liquidity/data-quality) built ahead of A3/A4 rather than after, since a live audit run against the real portfolio confirmed concentration risk isn't theoretical — yfinance currently can't price Zhipu or the 2x SK Hynix ETF (biggest real HK concentration risks at 30%/23%), which the new stale-price flag catches rather than silently hiding via cost-basis fallback. `universe.currency_for()` added (derives from ticker suffix) and surfaced a live bug in passing: TPZ.TO was being rejected by the universe gate (`.TO` never in `ELIGIBLE_SUFFIXES`) — fixed. Peer map extracted from A2 into `core/a5_relative_value/peers.py` so A2/A3/A5 share one map. 118 tests passing, live bot unaffected throughout · `core/a3_catalyst_response/`, `core/a4_portfolio_decision/`, `core/a5_relative_value/`, `core/risk/`, `infrastructure/b4_knowledge_base/decision_history.py`, `infrastructure/b1_universe/universe.py`
- **2026-07-08** — equity_ls capability-based spec agreed with Louis (5 core functions, 5 infra pieces incl. Discovery Pool + Decision History + B5 run modes, 8-check risk layer). Pre-build audit found 2 real bugs: (1) `debt_to_equity` from yfinance is percentage-point scale not a true ratio — normalized once in `b2_data_source/data_sources.py`, fixes NVDA/MU false "high leverage" flags. (2) screener `_handoff()` and `cadence.should_trigger_deep_dive()` were independent authorities on deep-dive escalation and disagreed for every T0/T1 name — `_handoff()` now delegates to cadence (`is_major_event()` helper added), `monitor.py` reads `sr.handoff_trigger` instead of recomputing. Decided: build backstage (pure logic, testable, no cron/Telegram) before any frontstage wiring — Decision History + single-writer rule next, then currency tags + minimal C layer, then A3, then A4 · `core/screener/`, `core/monitor/`, `infrastructure/b2_data_source/`
- **2026-07-07** — 6 European names added to watchlist from Bloomberg Core 6 (+ BAE BA.L → Defence) (ASML already held): Rheinmetall RHM.DE → Defence & Aerospace; Schneider SU.PA → AI-Energy (datacenter electrification consensus); Novo NVO + LVMH MC.PA + Nestle NESN.SW → new "Europe Blue Chips" group. 115 names, 18 groups, zero uncategorised; PDF regenerated · Notion + `src/tools/scheduler.py`

- **2026-07-07** — Categories restructured to Jensen's 5-layer AI stack per Louis: AI-Energy / AI-Chips (logic+foundry+semicap+memory incl. 2x Hynix ETF) / AI-Infrastructure (optics+networking+AI cloud; ORCL & CRWV moved here per market consensus) / AI-Models (Zhipu, MiniMax — split out of China Tech) / AI-Apps (sub-tags: cybersecurity, data platforms). Mag 7 unchanged; China Internet keeps platform names; non-AI groups unchanged. 17 groups, 110 names, zero uncategorised. Sub-layer detail lives as per-name tags in the spec PDF (~/Desktop/Portfolio_Groups_Proposal.pdf, now data-driven from live config) · `src/tools/scheduler.py`

- **2026-07-07** — HK book reconciled from broker: 10 positions written to Notion (4 existing rows got shares/cost: Zhipu, Zijin, JD Health, Tencent; 6 new rows: Ping An, Alibaba-HK, AIA, CSOP 2x SK Hynix, Meituan, Kuaishou). Notion DB has no Account property — Region select is the market field (existing rows use 'Greater China'). Alert categories updated (7709→Memory, insurers→Financials, 9988/1024→China Tech, 3690→E-Commerce). System now 110 names, 52 held. HK account: NAV HK$1.06M, ~96% invested, no leverage; Zhipu 30.5% + 2x Hynix ETF 23.1% = half the HK book · Notion + `src/tools/scheduler.py`

- **2026-07-07** — SPCX position closed per Louis (Notion Shares → 0, row kept for tracking; portfolio.db re-seeded, 42 held = exact broker match). Account snapshot noted for risk context: NAV $1.87M, positions $3.12M (~1.67x leverage), cash −$1.25M margin loan, buying power $621k, remaining liquidity $622k · Notion

- **2026-07-07** — Reconciled US broker statement vs system: 41/42 positions matched exactly; UBER (250 @ 75.50) was missing → added to Notion (sector "EV & Auto"), re-seeded portfolio.db (now 104 rows), added to EV & Future Mobility alert category. SPCX absent from broker (private position) — kept · Notion + `src/tools/scheduler.py`

- **2026-07-06** — Portfolio 分组 review with Louis: PORTFOLIO_CATEGORIES rebuilt from 11 stale groups (GE in Banks, most names in Other) to 18 groups covering all 42 holdings + 61 watchlist names exactly once. Mag 7 split out as its own group per Louis (trades as one macro factor); NVDA/TSLA/mega-caps moved there; GE → Defence & Aerospace. Spec PDF: ~/Desktop/Portfolio_Groups_Proposal.pdf. Open items: CBRS unidentified; Xiaomi in China Tech not EV; IREN in Crypto not AI Energy · `src/tools/scheduler.py`

- **2026-07-06** — Live bot: open-alert positions section now grouped by sector/theme (reuses `_categorise` + `PORTFOLIO_CATEGORIES`, same as close alert); biggest pre-market mover sorts first within each group; relies on send_telegram 4096-char auto-split for the long US list · `src/tools/scheduler.py`

- **2026-07-06** — Live bot: open-alert news section was SEO noise (chart pages, nav chrome, stale articles). Rebuilt `_fetch_market_news`: Tavily `topic="news"` + `days=2` (real articles only, all dated) → `clean_news` → DeepSeek compresses to ≤4 '• fact — implication' bullets; returns empty block when nothing solid (empty beats gibberish, per Louis). `tavily_search` gained optional topic/days params · `src/tools/scheduler.py`, `src/tools/llm.py`

- **2026-07-04** — Live bot: market open/close alerts fired on Jul 3 (NYSE closed, observed Jul 4). Added holiday gating via `holidays` pkg — NYSE financial calendar for US (handles observed shifts), country calendars as proxies for HK/CN/TW/KR/DE/GB; guards both alert functions and `_open_markets()` (so intraday jobs gated on `_is_market_open` also pause); check fails open so a calendar error can never suppress a real trading-day alert · `src/tools/scheduler.py`

- **2026-07-02** — Full audit of `src/desks/equity_ls/`: fixed monitor never screening T0/T1 (added per-ticker last-run state in `monitor_state.db`), screener 12M momentum (1y fetch can't cover 252 trading days → 2y, with 52w-high kept at tail(252)), earnings-date key mismatch (`next_earnings_date` vs B2's `earnings_date` — catalyst scoring + event triggers never fired), EDGAR field mapping (`form_type`/`entity_id` don't exist → `form`/`ciks`+`adsh`), B5 SEC context reading list as dict, FTS5 external-content sync moved to triggers (plain DELETE corrupted index), `ORDER BY created_at` tiebroken by `id` (8 same-second inserts made "latest" random), all SQLite modules close connections via `_db()` contextmanager, A2 verdict split into canonical `verdict` + `verdict_detail` (multiline text broke monitor alert filtering), shared `infrastructure/llm.py` (timeout=90s, retries=2 — debate rounds could hang forever), exclusion `add()` upserts reason, ETF returns lru_cached, screener `raw` strips DataFrame baggage · 40 tests in `tests/test_equity_ls_desk.py`; orphaned `tests/backtesting` (imported moved `src.backtesting`) archived to `legacy/superseded/tests_backtesting/`
- **2026-07-02** — equity_ls A2 valuation view grounded with live peer-multiples table (25+ ticker peer map + sector fallback) injected into prompt · `core/a2_deep_dive/deep_dive.py`
- **2026-07-01** — equity_ls screener extracted to `core/screener/` (A2, monitor, and future A1 all consume it); Step-7 review cadence as `cadence.py`; market monitor loop stubbed in `core/monitor/` — monitor decides *when*, screener decides *what score*, A2 decides *what view*
- **2026-07-01** — equity_ls B5 vendored TradingAgents (TauricResearch, Apache-2.0): prompts lifted into our own `pipeline.py` running on B2 data + DeepSeek; original files kept read-only in `vendored/`; outputs persist to B4 · `infrastructure/b5_trading_agents/`
- **2026-07-01** — equity_ls B4 knowledge base: reports w/ FTS5 search, versioned thesis history, agent outputs, `pdf_inbox/` drop-folder ingestion (pdfplumber, filename → ticker/type) · `infrastructure/b4_knowledge_base/`
- **2026-07-01** — equity_ls infrastructure reorganised into numbered subfolders `b1_universe / b2_data_source / b3_portfolio / b4_knowledge_base / b5_trading_agents`; core functions into `a1..a5` + `screener` + `monitor`
- **2026-06-29** — House View desk: run_house_view (A persona + B debate + C trader → synthesize → Recommendation) + pm_view button (one-call advisory verdict); degrade to HOLD/low when LLM down; not wired to triggers · `src/features/house_view/desk.py`
- **2026-06-29** — House View synthesize_view: persona direction avg + debate lean + quant tiebreak → Action; conviction from agreement; size from trader lens (risk-capped); disagreement surfaced · `src/features/house_view/synthesize.py`
- **2026-06-29** — House View lens B (bull/bear debate, JSON, degrade balanced) + lens C (trader sizing rule, honours risk CAP as hard constraint, no size_position import) · `src/features/house_view/lenses.py`
- **2026-06-29** — House View lens A: persona panel via LLMAdapter + recommendations.py prompts → 4 PersonaCalls (JSON), degrade to hold/Low · `src/features/house_view/lenses.py`
- **2026-06-29** — Added House View desk-local types (PersonaCall, DebateView, SizeView) · `src/features/house_view/types.py`
- **2026-06-29** — Risk Watch vet_decision: binding cap by current weight — buy/add to a >10% name returns CAP signal, else within-limits; size-string parsing deliberately avoided · `src/features/risk/desk.py`
- **2026-06-29** — Risk Watch desk: run_risk_sweep emits RISK Signals (single-name >10% concentration, >0.70 correlation pairs, macro regime); degrade_to DATA signal when no prices · `src/features/risk/desk.py`
- **2026-06-29** — Added RiskDataAdapter (regime via ficc.get_macro_regime + correlation>0.70 via yfinance, US equities only); on_failure → empty corr + UNKNOWN regime; deliberately avoids scheduler-coupled risk.py · `src/adapters/risk_data.py`
- **2026-06-29** — Quant Engine desk: run_quant_screen converts factor ranks → THEME Signals (severity from composite, RSI caveat for overbought/oversold); degrade_to DATA signal · `src/features/quant/desk.py`
- **2026-06-29** — Added QuantAdapter wrapping score_universe → list[dict]; on_failure returns [] · `src/adapters/quant.py`
- **2026-06-29** — ARCHITECTURE PIVOT (Louis's decision): adopting the asset-class desk model from `docs/DESKS.md` (9 desks: equity_ls/macro/credit/commodities/options_vol/crypto/event/quant + pm_risk orchestrator) over the prior process-cut 6-desk model. I advised against (most desks cover markets not actively traded; replaces canonical objects with IdeaCard) — Louis chose it. Executing STRANGLER-style: new `src/desks/` package added ALONGSIDE the live bot + 6 function-desks; nothing deleted. Built the foundation first per DESKS.md §2: `src/desks/contracts.py` (IdeaCard pydantic + Direction/Expression/TimeHorizon/IdeaStatus enums; sizing fields deliberately omitted — pm_risk owns sizing), `src/desks/base.py` (Desk ABC implementing the 11-step spine as a plain class — LangGraph wrapping deferred), `src/desks/registry.py` (DESK_IDS + register/get_desk). Copied spec to `docs/DESKS.md`; added CLAUDE.md pointer. Verified: IdeaCard validates, conviction 0-1 bound enforced, Desk.run() spine works. Prior BLUEPRINT/6-desk specs marked superseded-but-not-retired. OPEN DECISIONS before building individual desks: (a) LangGraph subgraphs vs plain classes, (b) which desks to actually instantiate given equity-focused book, (c) keep canonical objects as pm_risk's internal model or fully replace.
- **2026-06-29** — Ran read-only TradingAgents vendoring spike → `docs/desks/tradingagents_vendoring_map.md`. Inspected their real code + AgentState. Findings: (1) the 4 analysts are fully INDEPENDENT (each reads only ticker/date + own data tools, never another analyst's report) → clean lift; (2) bull/bear researchers call NO data tools (pure LLM reading the 4 reports + debate count loop) → no dependency baggage, only orchestration; (3) their 4 reports map 1:1 onto desks (fundamentals→Coverage, market/technical→Quant, news+sentiment→Idea Scout) so vendoring aligns with existing inter-desk flow. Grades: Coverage Fundamentals = clean lift (swap data tools→FundamentalsAdapter); Idea Scout News+Sentiment = clean lift (swap→Tavily/FRED adapters); House View Bull/Bear+Manager+Trader = lift-with-stub (feed reports + run count loop). Corrects earlier warning: data-tool baggage applies to analysts, NOT to House View's debate. Recommended order: news→Coverage seam → Coverage → Idea Scout → House View. Decisions pending for House View: debate round count, reflection/memory defer, manager-vs-synthesize_view. No code changed.
- **2026-06-29** — Wrote `WORK_ORDER_HOUSE_VIEW.md` (House View onto desk model, on-demand). 5 literal tasks: HV1 desk-local types (PersonaCall/DebateView/SizeView), HV2 lens A persona panel (LLMAdapter + recommendations.py prompts → 4 calls), HV3 lens B bull/bear debate + lens C trader sizing (rule-based, honours Risk CAP as hard constraint, no size_position import), HV4 synthesize_view (persona avg + debate lean + quant tiebreak → Action; conviction from agreement; disagreement surfaced), HV5 run_house_view orchestrator + pm_view button (one-call advisory verdict). Grounded: recommendations.py persona prompts import clean; size_position is in live telegram_bot.py so deliberately NOT imported. No dispatcher/triggers/live-scheduler. All three inputs (Coverage Thesis, Quant Signal, Risk cap) now exist.
- **2026-06-29** — Verified Codex built WORK_ORDER_QUANT (QE1-2) + WORK_ORDER_RISK (RW1-3); all verifications re-run and pass (QuantAdapter 13 cols, run_quant_screen 5 signals MU BUY rank 1, run_risk_sweep concentration 63%/37% + regime). Fixed one safety gap I left in the RW3 spec: `vet_decision` returned a false "within limits (0.0%)" green when price data was missing (total==0) — now returns a DATA "could not assess — price data unavailable" signal instead, per Risk Watch spec §7/§9 (flag, never suppress). Codex's code was correct to the (incomplete) spec; gap was mine. · `src/features/risk/desk.py`
- **2026-06-29** — Wrote `WORK_ORDER_RISK.md` (Risk Watch onto desk model, on-demand). 3 literal tasks: RW1 RiskDataAdapter (regime via ficc + correlation>0.70 via yfinance, US equities only — deliberately avoids scheduler-coupled risk.py), RW2 run_risk_sweep (single-name >10% concentration + correlation pairs + regime → RISK Signals, degrade_to DATA when no prices), RW3 vet_decision (binding cap by current weight, avoids parsing free-text size). No dispatcher/triggers/live-scheduler. Key grounding: risk.py imports PORTFOLIO_CATEGORIES from live scheduler at module load, so the work order forbids importing it and reimplements correlation in the adapter.
- **2026-06-29** — Wrote `WORK_ORDER_QUANT.md` (Quant Engine onto desk model, on-demand). 2 literal tasks: QE1 QuantAdapter (wraps score_universe → list[dict], on_failure []), QE2 quant desk run_quant_screen (rank rows → THEME Signals, severity from composite, RSI overbought/oversold caveat, degrade_to DATA signal). Verified clean import of factors.score_universe (no scheduler side effects). No dispatcher/triggers/live-scheduler. Decision: no dedicated quant SignalType — reuse THEME with source_desk=quant_engine.
- **2026-06-29** — Wrote full Risk Watch desk spec → `docs/desks/risk_watch.md` (all 13 sections). Decisions: (1) mechanical but always-on cross-cutting desk — single best method (native V3 risk engine Phase 1: concentration/correlation/drawdown/regime), not A/B/C; (2) borrowed role = TradingAgents Risk-Management-Team / PM aggregation STRUCTURE as reference only — the 3-debater committee is deferred per blueprint until D2/D4 solid; (3) vets EVERY House View Recommendation and returns a BINDING max-size/reduce flag (risk constrains, House View obeys); (4) correlated names (>0.7) collapsed into one effective position before concentration judged; risk is a MAX not an average (one red breach not softened by greens); (5) cross-cutting — constrains within the decision hop, never extends the chain past 3; degrade_to = raw concentration-by-weight from Notion when no market data. 3 open questions (concentration limit numbers, binding-vs-advisory, Phase-2 priority). ALL SIX DESK SPECS NOW COMPLETE.
- **2026-06-29** — Wrote full Quant Engine desk spec → `docs/desks/quant_engine.md` (all 13 sections). Decisions: (1) mechanical desk — single best method (native V3 Quant factor model + Qlib backtest patterns + TradingAgents Technical-Analyst signal layer as reference), not A/B/C; (2) borrowed role = Technical Analyst (MACD/RSI/trend) added as a NEW systematic lens the system lacked; (3) technical layer is a *caveat overlay* on the factor rank, not a vote — "top rank but trend broken" surfaced, not merged; (4) feeds House View (rank/weight as sizing+tiebreaker) and Risk Watch (optimiser weights/exposure) — not pull-only; (5) optimiser proposes, Risk constrains; backtest is strategy evidence, never a per-name call; (6) universe capped 98/~500, 24h rank cache, hop-2 input, cap 3. 3 open questions (technical weight, rebalance authority, default universe).
- **2026-06-29** — Wrote full House View (CIO) desk spec → `docs/desks/house_view.md` (all 13 sections). Confirmed split with Louis: Bull/Bear + Trader live HERE (D4), Fundamentals/Technical stay in Coverage (D2) — Coverage = "what is true", House View = "what to do". Decisions: (1) A/B/C all vendored — A persona panel (ai-hedge-fund), B bull/bear debate (TradingAgents), C trader sizing (TradingAgents, sizing logic ONLY, no autonomous execution — PM seat stays human); (2) added the **PM View button** as a lightweight pull trigger — one LLM call reading on-screen context + Notion position, returns the tight 🧠 PM Verdict card, advisory only, distinct from the full A/B/C run; (3) consumes Quant rank (tiebreaker) + Risk Watch max-size (hard cap); writes Recommendation to decision journal only when actionable; (4) terminal opinion desk — emits to user+journal, never onward; sits at hop 2-3, respects cap 3; (5) disagreement always shown, conviction scales with agreement. Also flipped Coverage Analyst "Spec written" 🟡→✅ (its spec predates this session). 3 open questions (PM-View journal option, auto-journal threshold, sizing authority).
- **2026-06-29** — Refined Research Librarian §6 Sources per Louis: LlamaIndex [library — RAG/chunking]; TradingAgents [reference] cribbing three specific patterns — `dataflows/` (fetch→structure), `news_analyst.py` (content ingestion), `reporting.py` (output template); FinGPT [reference — summarisation prompts]. No A/B/C — single best implementation. · `docs/desks/research_librarian.md`
- **2026-06-29** — Wrote full Idea Scout desk spec → `docs/desks/idea_scout.md` (all 13 sections). Decisions: (1) opinion desk borrowing TradingAgents News Analyst (feed A) + Sentiment Analyst (feed B), with native theme/momentum as feed C, OpenBB as the data floor; (2) A/B/C are signal *feeds* — promotion bar = ≥2 feeds agree → route to Coverage; single feed = "watch"; (3) disagreement (news-hot vs crowded) is surfaced in output, never averaged; precedence C=thesis frame, A=urgency, B=crowding caveat; (4) routes promotions to Coverage Analyst and pulls evidence packs from Research Librarian — not pull-only; (5) candidate expressed as `Name`+`Signal` (no new canonical object); (6) Cooldown 7-day per name, max 2 promotions/day; (7) hop 1 (own scan) or hop 2 (via Librarian), cap 3 hops. 3 open questions (Candidate object, promotion bar, hunt universe). Also updated Research Librarian spec: added inter-desk pull trigger (evidence pack on request) + named its borrowed role (data/report-retrieval layer).
- **2026-06-29** — Wrote full Research Librarian desk spec → `docs/desks/research_librarian.md` (all 13 template sections). Decisions made: (1) mechanical desk — single best implementation (LlamaIndex library + TradingAgents News-Analyst / FinGPT as reference), not A/B/C; (2) it is NOT pull-only — routes a THESIS Signal to Coverage Analyst for covered-name mentions and a NEWS Signal to Idea Scout for uncovered names; (3) within-report factual conflicts are surfaced not resolved (reliability signal); (4) Cooldown = max one Coverage re-test per covered name per 24h; (5) dedup by raw-bytes report hash; (6) Librarian is chain hop 1 (entry point, never triggered by another desk), max 3 hops total. 3 open questions flagged (intake channel, routing aggressiveness, retention).
- **2026-06-29** — End-to-end Coverage run on MU with real evidence (FundamentalsAdapter → EvidenceService → 3 lenses → synthesize); runs clean · verification only
- **2026-06-29** — Lens A real: TradingAgents fundamentals prompt pattern via LLMAdapter → per-driver holding/strained/invalidated JSON; degrade paths for LLM error + unparseable · `src/features/coverage/lenses.py`
- **2026-06-29** — Lens B real: ported Damodaran FCFF DCF + margin-of-safety from src/agents/aswath_damodaran.py to read Evidence.fundamentals; cheap/fair/expensive with P/E degrade path · `src/features/coverage/lenses.py`
- **2026-06-29** — Added EvidenceService.assemble(name_ref) → Evidence with fundamentals via FundamentalsAdapter · `src/services/evidence.py`
- **2026-06-29** — Added LLMAdapter wrapping call_deepseek; on_failure (falsy or ❌) returns "" · `src/adapters/llm.py`
- **2026-06-29** — Added FundamentalsAdapter (yfinance .info → 11-key normalised dict); on_failure returns {} · `src/adapters/fundamentals.py`
- **2026-06-29** — Added NOTICE.md attributing TradingAgents (Apache-2.0) and ai-hedge-fund (MIT) per vendoring duty · `NOTICE.md`
- **2026-06-29** — Wrote `WORK_ORDER_PHASE2.md` (Coverage real lenses) after Louis's decisions: data source = yfinance (financialdatasets deferred), LLM on (all 3 lenses real), §13 defaults stand. Literal tasks P1 NOTICE.md → P2 FundamentalsAdapter (yfinance .info, 11 keys) → P3 LLMAdapter (wraps call_deepseek) → P4 EvidenceService → P5 lens B real (ported Damodaran DCF + margin-of-safety, P/E degrade) → P6 lens A real (TradingAgents prompt → per-driver JSON) → P7 end-to-end on MU. Triggers + Notion write-back explicitly out of scope. Data shapes pre-verified against aswath_damodaran.py and call_deepseek signature.
- **2026-06-29** — Verified vendoring licences for Coverage lenses: TradingAgents = Apache-2.0 (raw LICENSE checked), virattt/ai-hedge-fund = MIT © 2024 Virat Singh (raw LICENSE checked). Both permit vendoring with attribution. Key finding: lens B's source (Damodaran agent) is already in-repo at `src/agents/aswath_damodaran.py` (this repo forks ai-hedge-fund) — adapt in place, no external copy. Wrote plan → `docs/desks/coverage_lenses_plan.md` (prereq chain: FundamentalsAdapter → LLMAdapter → EvidenceService → lens B → lens A; 3 decisions flagged for Louis: data source, LLM-now-or-defer, §13 confirm). No code yet — gated on decisions.
- **2026-06-29** — Re-executed WORK_ORDER Task 5 to the hardened spec: morning briefing now computes pnl_pct vs avg_cost (was reading the always-0 Position.pnl_pct), uses exact price-dict access (`.get("price")`), treats falsy price as "data pending", sorts by computed pnl descending. Verified: header `Morning Briefing — 2026-06-29`, top line `SNDK: $2090.71 (260.2% vs cost)`. File: `src/features/morning_briefing.py`.
- **2026-06-29** — Handover review: confirmed Codex completed WORK_ORDER Tasks 1–9 (adapters, morning briefing, full Coverage Analyst skeleton). All verification commands re-run and pass (synthesize → BROKEN/SELL and run_coverage → WEAKENING exactly as specified). No blockers raised. Reconciled CURRENT SPRINT item 3 status 🔴→🟡 (was stale; skeleton built, vendoring/triggers remain). One known gap: morning briefing was built to the pre-hardening Task 5 spec (shows 0.0% pnl) — next task for the worker is to redo it to the hardened spec.
- **2026-06-29** — Built run_coverage() orchestrator wiring 3 lenses → synthesize → CoverageResult; degrade_to documented; not wired to triggers · `src/features/coverage/desk.py`
- **2026-06-29** — Built Coverage synthesize() verdict state machine (§5/§7): core-invalidated→broken, strained→weakening; push only on →broken; rec carries action+rationale, no sizing · `src/features/coverage/synthesize.py`
- **2026-06-29** — Added Coverage lenses: C/pillars native (reads driver.status); A/fundamentals and B/valuation interface stubs (vendoring deferred pending licence check) · `src/features/coverage/lenses.py`
- **2026-06-29** — Added Coverage Analyst desk-local types (Evidence, LensView, CoverageResult); kept out of core/objects.py per spec §10 · `src/features/coverage/types.py`
- **2026-06-29** — Built spine+objects morning briefing as standalone reference (PortfolioAdapter + PriceAdapter → Position objects → string); not wired to live scheduler · `src/features/morning_briefing.py`
- **2026-06-29** — Added PortfolioAdapter producing Position objects from get_holdings_cached (held only, shares>0) · `src/adapters/portfolio.py`
- **2026-06-29** — Added PriceAdapter wrapping src.tools.prices.get_live_prices behind the adapter interface · `src/adapters/prices.py`
- **2026-06-29** — Added adapter base interface (Adapter ABC + AdapterError, on_failure required) · `src/adapters/base.py`
- **2026-06-29** — Handover audit of WORK_ORDER.md against live code shapes. Fixed: (1) Task 4 PortfolioAdapter referenced a non-existent `current_price` key in get_holdings_cached (real keys: account/avg_cost/name/rating/role/sector/shares/thesis) — removed, price now comes only from PriceAdapter; (2) Task 5 morning briefing used wrong failure mode (failed ticker is present-but-`{}`, not absent) and sorted by an always-0 field — rewrote with exact inner-key access (`price`/`change_pct`) and computable pnl_pct vs avg_cost; (3) reconciled coverage_analyst.md §13 open questions vs WORK_ORDER Tasks 7–8 so a literal worker won't STOP — §13 now defers to WORK_ORDER as authoritative provisional defaults. Files: WORK_ORDER.md, docs/desks/coverage_analyst.md.
- **2026-06-29** — Wrote `WORK_ORDER.md` (repo root) — an executable, literal-agent task list derived from CURRENT SPRINT + coverage_analyst.md. Tasks: 1 objects (done) → 2 adapter base → 3 PriceAdapter → 4 PortfolioAdapter → 5 morning-briefing reference → 6–9 Coverage Analyst (types, lenses, synthesize, orchestrator) → 10–11 deferred (not executable). Encoded provisional defaults for spec §13 open questions (read is_core from Driver; push only on →broken; rec carries action+rationale, no sizing). Listed in README folder layout. No code executed.
- **2026-06-29** — Added `ONBOARDING.md` to repo root as the single first-read entry point (orientation + read order + non-negotiable rules). Listed first in README folder layout ("← read this first"); referenced at the top of AGENTS.md "Where the truth lives" ("read before anything else").
- **2026-06-29** — Canonical objects coded → `src/core/objects.py`. 7 dataclasses (Name, Position, Thesis, Signal, Recommendation, Report, Event) + Driver, 6 enums (AssetClass, Verdict, DriverStatus, Action, SignalType, EventType), and a `to_dict()` serialiser (flattens enums/datetimes for Notion/SQLite). Decisions: (1) names referenced by ticker string (`name_ref`), not by Name instance — stable, serialisable, matches Notion/SQLite. (2) `Driver` promoted to its own object (id, summary, is_core, status, evidence) to support the Coverage Analyst per-driver state machine. (3) `Recommendation.size` kept free-text by design. (4) New code lives under `src/core/` (blueprint `core/` mapping, stays importable in `src` package). Verified: imports clean, round-trips through `to_dict`.
- **2026-06-29** — Adopted three-part architecture (Spine / Desks / Standards). Desks-first, then functions. Removed migration from the architecture map into this log. Multi-source per opinion desk (A/B/C) approved, with guardrail: sources are modules sharing canonical objects, not live frameworks. TradingAgents = vendored; ai-hedge-fund = vendored; OpenBB/Qlib/LlamaIndex = libraries; FinRobot/HKUDS repos = reference.

> Add one line per decision, before moving on.
